# 2026-05-01_20-24-59 多数据耦合分析报告

## 1. 数据含义

- `Act_q0..Act_q5`：六个关节的实际关节角。
- `Tgt_q0..Tgt_q5`：六个关节的目标角度，用于评估跟踪误差。
- `Act_qd0..Act_qd5`：六个关节的实际角速度，是平滑性和阶段切换的主运动信号。
- `Act_X/Y/Z` 与 `Act_RX/RY/RZ`：TCP 位置与姿态，用于描述末端空间状态。
- `Act_dX/dY/dZ` 与 `Act_dRX/dRY/dRZ`：TCP 线速度与角速度，用于描述末端空间运动强度。
- `Fx/Fy/Fz/Mx/My/Mz`：末端六维载荷，用于描述接触、装载、回撤和称重过程中的响应。

## 2. 数据合理性判断

- 关节跟踪整体稳定，最大 RMSE 为 `0.000545`。
- `J5` 的局部速度波动最强，差分标准差为 `0.019135`。
- 关节-载荷主耦合为 `Act_qd0` 与 `Fz`，`r=-0.4941`。
- TCP-载荷主耦合为 `dX` 与 `Fz`，`r=-0.5822`。
- 阶段识别存在缺失：`接触`。该结果仍可用，但建议后期人工微调。

## 3. 阶段分段结果

| Stage | Status | Sample Range | Confidence |
| --- | --- | --- | --- |
| 接触 | missing | N/A | low |
| 装载 | identified | 220 -> 380 | medium |
| 回撤 | identified | 381 -> 813 | high |
| 称重 | identified | 814 -> 1184 | high |

- `接触`：未稳定识别，允许缺失，建议人工复核。
- `装载`：样本 `220 -> 380`，关节运动均值 `0.3218`，TCP 运动均值 `0.2830`，力幅值均值 `68.5856`，可信度 `medium`。依据载荷持续增高并达到称重前峰值确定装载段。
- `回撤`：样本 `381 -> 813`，关节运动均值 `0.6638`，TCP 运动均值 `0.5027`，力幅值均值 `65.8395`，可信度 `high`。依据称重前持续运动与载荷回落趋势确定回撤段。 结合已有参考稳态窗口提高可信度。
- `称重`：样本 `814 -> 1184`，关节运动均值 `0.0289`，TCP 运动均值 `0.0259`，力幅值均值 `55.7251`，可信度 `high`。复用已有称重分析中的最终稳态段。

## 4. 平滑性与耦合摘要

- 样本总数：`1209`
- 最大关节跟踪 RMSE：`0.000545`
- 局部波动最强关节：`J5`，其 `Diff SD=0.019135`。

| Rank | 关节-载荷耦合 | Pearson r |
| --- | --- | ---: |
| 1 | `Act_qd0` vs `Fz` | -0.4941 |
| 2 | `Act_qd0` vs `Mz` | 0.4596 |
| 3 | `Act_qd0` vs `Fy` | -0.4444 |
| 4 | `Act_qd0` vs `My` | -0.3927 |
| 5 | `Act_qd3` vs `Fx` | -0.3866 |
| 6 | `Act_qd0` vs `Mx` | 0.3579 |

| Rank | TCP-载荷耦合 | Pearson r |
| --- | --- | ---: |
| 1 | `dX` vs `Fz` | -0.5822 |
| 2 | `dX` vs `Fy` | -0.5517 |
| 3 | `dX` vs `Mz` | 0.5422 |
| 4 | `dX` vs `My` | -0.4317 |
| 5 | `dRY` vs `Fx` | -0.3671 |
| 6 | `dX` vs `Mx` | 0.3532 |

## 5. 输出文件

- `main_figure.png / .svg`：主图
- `supplementary_figure.png / .svg`：补充图

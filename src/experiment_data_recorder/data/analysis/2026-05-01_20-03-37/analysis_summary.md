# 2026-05-01_20-03-37 多数据耦合分析报告

## 1. 数据含义

- `Act_q0..Act_q5`：六个关节的实际关节角。
- `Tgt_q0..Tgt_q5`：六个关节的目标角度，用于评估跟踪误差。
- `Act_qd0..Act_qd5`：六个关节的实际角速度，是平滑性和阶段切换的主运动信号。
- `Act_X/Y/Z` 与 `Act_RX/RY/RZ`：TCP 位置与姿态，用于描述末端空间状态。
- `Act_dX/dY/dZ` 与 `Act_dRX/dRY/dRZ`：TCP 线速度与角速度，用于描述末端空间运动强度。
- `Fx/Fy/Fz/Mx/My/Mz`：末端六维载荷，用于描述接触、装载、回撤和称重过程中的响应。

## 2. 数据合理性判断

- 关节跟踪整体稳定，最大 RMSE 为 `0.000549`。
- `J5` 的局部速度波动最强，差分标准差为 `0.016851`。
- 关节-载荷主耦合为 `Act_qd0` 与 `Fy`，`r=-0.5892`。
- TCP-载荷主耦合为 `dX` 与 `Fy`，`r=-0.6077`。
- 四类阶段均已识别，分段结构完整。

## 3. 阶段分段结果

| Stage | Status | Sample Range | Confidence |
| --- | --- | --- | --- |
| 接触 | identified | 149 -> 339 | medium |
| 装载 | identified | 340 -> 520 | medium |
| 回撤 | identified | 521 -> 804 | high |
| 称重 | identified | 805 -> 1193 | high |

- `接触`：样本 `149 -> 339`，关节运动均值 `0.4877`，TCP 运动均值 `0.2809`，力幅值均值 `63.3778`，可信度 `medium`。依据早期运动启动与载荷显著上升确定接触段。
- `装载`：样本 `340 -> 520`，关节运动均值 `0.5074`，TCP 运动均值 `0.4108`，力幅值均值 `63.6330`，可信度 `medium`。依据载荷持续增高并达到称重前峰值确定装载段。
- `回撤`：样本 `521 -> 804`，关节运动均值 `0.5190`，TCP 运动均值 `0.4222`，力幅值均值 `59.0635`，可信度 `high`。依据称重前持续运动与载荷回落趋势确定回撤段。 结合已有参考稳态窗口提高可信度。
- `称重`：样本 `805 -> 1193`，关节运动均值 `0.0005`，TCP 运动均值 `0.0005`，力幅值均值 `52.5333`，可信度 `high`。复用已有称重分析中的最终稳态段。

## 4. 平滑性与耦合摘要

- 样本总数：`1192`
- 最大关节跟踪 RMSE：`0.000549`
- 局部波动最强关节：`J5`，其 `Diff SD=0.016851`。

| Rank | 关节-载荷耦合 | Pearson r |
| --- | --- | ---: |
| 1 | `Act_qd0` vs `Fy` | -0.5892 |
| 2 | `Act_qd0` vs `Mz` | 0.5849 |
| 3 | `Act_qd0` vs `Fz` | -0.4927 |
| 4 | `Act_qd4` vs `Mz` | -0.4291 |
| 5 | `Act_qd0` vs `My` | -0.4264 |
| 6 | `Act_qd4` vs `Fy` | 0.4170 |

| Rank | TCP-载荷耦合 | Pearson r |
| --- | --- | ---: |
| 1 | `dX` vs `Fy` | -0.6077 |
| 2 | `dX` vs `Mz` | 0.6062 |
| 3 | `dX` vs `Fz` | -0.5233 |
| 4 | `dRY` vs `Fx` | 0.4781 |
| 5 | `dX` vs `My` | -0.4133 |
| 6 | `dRY` vs `Mz` | 0.3748 |

## 5. 输出文件

- `main_figure.png / .svg`：主图
- `supplementary_figure.png / .svg`：补充图

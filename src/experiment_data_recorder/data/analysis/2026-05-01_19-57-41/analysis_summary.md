# 2026-05-01_19-57-41 多数据耦合分析报告

## 1. 数据含义

- `Act_q0..Act_q5`：六个关节的实际关节角。
- `Tgt_q0..Tgt_q5`：六个关节的目标角度，用于评估跟踪误差。
- `Act_qd0..Act_qd5`：六个关节的实际角速度，是平滑性和阶段切换的主运动信号。
- `Act_X/Y/Z` 与 `Act_RX/RY/RZ`：TCP 位置与姿态，用于描述末端空间状态。
- `Act_dX/dY/dZ` 与 `Act_dRX/dRY/dRZ`：TCP 线速度与角速度，用于描述末端空间运动强度。
- `Fx/Fy/Fz/Mx/My/Mz`：末端六维载荷，用于描述接触、装载、回撤和称重过程中的响应。

## 2. 数据合理性判断

- 关节跟踪整体稳定，最大 RMSE 为 `0.000109`。
- `J2` 的局部速度波动最强，差分标准差为 `0.005647`。
- 关节-载荷主耦合为 `Act_qd1` 与 `Fy`，`r=-0.6694`。
- TCP-载荷主耦合为 `dZ` 与 `Fy`，`r=0.6803`。
- 阶段识别存在缺失：`接触`。该结果仍可用，但建议后期人工微调。

## 3. 阶段分段结果

| Stage | Status | Sample Range | Confidence |
| --- | --- | --- | --- |
| 接触 | missing | N/A | low |
| 装载 | identified | 447 -> 1074 | medium |
| 回撤 | identified | 1075 -> 2618 | high |
| 称重 | identified | 2619 -> 3380 | high |

- `接触`：未稳定识别，允许缺失，建议人工复核。
- `装载`：样本 `447 -> 1074`，关节运动均值 `0.1524`，TCP 运动均值 `0.0887`，力幅值均值 `63.7067`，可信度 `medium`。依据载荷持续增高并达到称重前峰值确定装载段。
- `回撤`：样本 `1075 -> 2618`，关节运动均值 `0.1512`，TCP 运动均值 `0.1238`，力幅值均值 `53.6755`，可信度 `high`。依据称重前持续运动与载荷回落趋势确定回撤段。 结合已有参考稳态窗口提高可信度。
- `称重`：样本 `2619 -> 3380`，关节运动均值 `0.0050`，TCP 运动均值 `0.0035`，力幅值均值 `54.2573`，可信度 `high`。复用已有称重分析中的最终稳态段。

## 4. 平滑性与耦合摘要

- 样本总数：`3404`
- 最大关节跟踪 RMSE：`0.000109`
- 局部波动最强关节：`J2`，其 `Diff SD=0.005647`。

| Rank | 关节-载荷耦合 | Pearson r |
| --- | --- | ---: |
| 1 | `Act_qd1` vs `Fy` | -0.6694 |
| 2 | `Act_qd1` vs `Fz` | -0.6410 |
| 3 | `Act_qd5` vs `Fz` | 0.6151 |
| 4 | `Act_qd5` vs `Mz` | -0.5602 |
| 5 | `Act_qd4` vs `Fx` | -0.5559 |
| 6 | `Act_qd1` vs `Mz` | 0.5551 |

| Rank | TCP-载荷耦合 | Pearson r |
| --- | --- | ---: |
| 1 | `dZ` vs `Fy` | 0.6803 |
| 2 | `dZ` vs `Fz` | 0.6613 |
| 3 | `dRY` vs `Fx` | 0.6405 |
| 4 | `dRX` vs `Mz` | 0.6357 |
| 5 | `dRX` vs `Fz` | -0.6234 |
| 6 | `dZ` vs `Mz` | -0.6137 |

## 5. 输出文件

- `main_figure.png / .svg`：主图
- `supplementary_figure.png / .svg`：补充图

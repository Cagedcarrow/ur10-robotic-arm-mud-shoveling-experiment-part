# 2026-05-01_20-23-41 多数据耦合分析报告

## 1. 数据含义

- `Act_q0..Act_q5`：六个关节的实际关节角。
- `Tgt_q0..Tgt_q5`：六个关节的目标角度，用于评估跟踪误差。
- `Act_qd0..Act_qd5`：六个关节的实际角速度，是平滑性和阶段切换的主运动信号。
- `Act_X/Y/Z` 与 `Act_RX/RY/RZ`：TCP 位置与姿态，用于描述末端空间状态。
- `Act_dX/dY/dZ` 与 `Act_dRX/dRY/dRZ`：TCP 线速度与角速度，用于描述末端空间运动强度。
- `Fx/Fy/Fz/Mx/My/Mz`：末端六维载荷，用于描述接触、装载、回撤和称重过程中的响应。

## 2. 数据合理性判断

- 关节跟踪整体稳定，最大 RMSE 为 `0.000244`。
- `J2` 的局部速度波动最强，差分标准差为 `0.007517`。
- 关节-载荷主耦合为 `Act_qd5` 与 `Mx`，`r=-0.6363`。
- TCP-载荷主耦合为 `dZ` 与 `Fy`，`r=0.6478`。
- 阶段识别存在缺失：`接触`。该结果仍可用，但建议后期人工微调。

## 3. 阶段分段结果

| Stage | Status | Sample Range | Confidence |
| --- | --- | --- | --- |
| 接触 | missing | N/A | low |
| 装载 | identified | 183 -> 427 | medium |
| 回撤 | identified | 428 -> 1286 | high |
| 称重 | identified | 1287 -> 1525 | high |

- `接触`：未稳定识别，允许缺失，建议人工复核。
- `装载`：样本 `183 -> 427`，关节运动均值 `0.1807`，TCP 运动均值 `0.1612`，力幅值均值 `69.4293`，可信度 `medium`。依据载荷持续增高并达到称重前峰值确定装载段。
- `回撤`：样本 `428 -> 1286`，关节运动均值 `0.3343`，TCP 运动均值 `0.2526`，力幅值均值 `60.2060`，可信度 `high`。依据称重前持续运动与载荷回落趋势确定回撤段。 结合已有参考稳态窗口提高可信度。
- `称重`：样本 `1287 -> 1525`，关节运动均值 `0.0193`，TCP 运动均值 `0.0171`，力幅值均值 `53.8734`，可信度 `high`。复用已有称重分析中的最终稳态段。

## 4. 平滑性与耦合摘要

- 样本总数：`1555`
- 最大关节跟踪 RMSE：`0.000244`
- 局部波动最强关节：`J2`，其 `Diff SD=0.007517`。

| Rank | 关节-载荷耦合 | Pearson r |
| --- | --- | ---: |
| 1 | `Act_qd5` vs `Mx` | -0.6363 |
| 2 | `Act_qd4` vs `Fx` | -0.6162 |
| 3 | `Act_qd5` vs `My` | 0.5963 |
| 4 | `Act_qd4` vs `Mx` | 0.5876 |
| 5 | `Act_qd0` vs `Fy` | -0.5874 |
| 6 | `Act_qd5` vs `Fx` | 0.5852 |

| Rank | TCP-载荷耦合 | Pearson r |
| --- | --- | ---: |
| 1 | `dZ` vs `Fy` | 0.6478 |
| 2 | `dRX` vs `Mz` | 0.6403 |
| 3 | `dRX` vs `Fz` | -0.6291 |
| 4 | `dZ` vs `Mz` | -0.5990 |
| 5 | `dZ` vs `Fz` | 0.5893 |
| 6 | `dX` vs `Fy` | -0.5758 |

## 5. 输出文件

- `main_figure.png / .svg`：主图
- `supplementary_figure.png / .svg`：补充图

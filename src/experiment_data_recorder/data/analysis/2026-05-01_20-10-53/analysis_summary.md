# 2026-05-01_20-10-53 多数据耦合分析报告

## 1. 数据含义

- `Act_q0..Act_q5`：六个关节的实际关节角。
- `Tgt_q0..Tgt_q5`：六个关节的目标角度，用于评估跟踪误差。
- `Act_qd0..Act_qd5`：六个关节的实际角速度，是平滑性和阶段切换的主运动信号。
- `Act_X/Y/Z` 与 `Act_RX/RY/RZ`：TCP 位置与姿态，用于描述末端空间状态。
- `Act_dX/dY/dZ` 与 `Act_dRX/dRY/dRZ`：TCP 线速度与角速度，用于描述末端空间运动强度。
- `Fx/Fy/Fz/Mx/My/Mz`：末端六维载荷，用于描述接触、装载、回撤和称重过程中的响应。

## 2. 数据合理性判断

- 关节跟踪整体稳定，最大 RMSE 为 `0.000103`。
- `J2` 的局部速度波动最强，差分标准差为 `0.005623`。
- 关节-载荷主耦合为 `Act_qd1` 与 `Fz`，`r=-0.6686`。
- TCP-载荷主耦合为 `dZ` 与 `Fz`，`r=0.7096`。
- 阶段识别存在缺失：`接触`。该结果仍可用，但建议后期人工微调。

## 3. 阶段分段结果

| Stage | Status | Sample Range | Confidence |
| --- | --- | --- | --- |
| 接触 | missing | N/A | low |
| 装载 | identified | 230 -> 266 | medium |
| 回撤 | identified | 267 -> 2017 | high |
| 称重 | identified | 2018 -> 2493 | high |

- `接触`：未稳定识别，允许缺失，建议人工复核。
- `装载`：样本 `230 -> 266`，关节运动均值 `0.0989`，TCP 运动均值 `0.0450`，力幅值均值 `64.7441`，可信度 `medium`。依据载荷持续增高并达到称重前峰值确定装载段。
- `回撤`：样本 `267 -> 2017`，关节运动均值 `0.1934`，TCP 运动均值 `0.1217`，力幅值均值 `60.4101`，可信度 `high`。依据称重前持续运动与载荷回落趋势确定回撤段。 结合已有参考稳态窗口提高可信度。
- `称重`：样本 `2018 -> 2493`，关节运动均值 `0.0123`，TCP 运动均值 `0.0056`，力幅值均值 `53.4847`，可信度 `high`。复用已有称重分析中的最终稳态段。

## 4. 平滑性与耦合摘要

- 样本总数：`2509`
- 最大关节跟踪 RMSE：`0.000103`
- 局部波动最强关节：`J2`，其 `Diff SD=0.005623`。

| Rank | 关节-载荷耦合 | Pearson r |
| --- | --- | ---: |
| 1 | `Act_qd1` vs `Fz` | -0.6686 |
| 2 | `Act_qd1` vs `Fx` | 0.5664 |
| 3 | `Act_qd1` vs `Fy` | -0.5609 |
| 4 | `Act_qd5` vs `Mx` | -0.5471 |
| 5 | `Act_qd2` vs `Fz` | 0.5260 |
| 6 | `Act_qd2` vs `Fx` | -0.5246 |

| Rank | TCP-载荷耦合 | Pearson r |
| --- | --- | ---: |
| 1 | `dZ` vs `Fz` | 0.7096 |
| 2 | `dZ` vs `Fy` | 0.6895 |
| 3 | `dX` vs `Fx` | 0.6174 |
| 4 | `dY` vs `Mx` | -0.5930 |
| 5 | `dY` vs `Fx` | 0.5666 |
| 6 | `dZ` vs `Mz` | -0.5616 |

## 5. 输出文件

- `main_figure.png / .svg`：主图
- `supplementary_figure.png / .svg`：补充图
